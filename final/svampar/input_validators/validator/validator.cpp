#include "validator.h"
#include <cstring>
#include <vector>

typedef long long ll;

const int N_IS = 512;
const int MAX_VAL = 255;
void run() {
    int n = Int(N_IS, N_IS);
    Endl();
    
    SpacedInts(n, 0, MAX_VAL);
}
